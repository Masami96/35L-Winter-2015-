--- timespec.h	2005-09-21 23:05:39.000000000 -0700
+++ fixedtimespec.h	2016-02-08 11:21:06.792954000 -0800
@@ -45,8 +45,13 @@
 static inline int
 timespec_cmp (struct timespec a, struct timespec b)
 {
-  int diff = a.tv_sec - b.tv_sec;
-  return diff ? diff : a.tv_nsec - b.tv_nsec;
+  signed long long int diff = (singed long long int) a.tv_sec - (signed long long int) b.tv_sec;
+  if (diff > 0)
+    return 1;
+  else if (diff < 0)
+    return -1;
+  else
+    return 0;
 }
 
 # if ! HAVE_DECL_NANOSLEEP
